<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fanqie.dc.domain.UserBean">
   <resultMap id="mapId" type="com.fanqie.dc.domain.UserBean">
        <result column="id" property="id"/>
        <result column="name" property="name"/>
   </resultMap>

    <insert id="save" parameterType="com.fanqie.dc.domain.UserBean">
        insert into dc_user(id,name)VALUES (#{id}, #{name})
    </insert>

    <select id="findAll" resultMap="mapId">
        SELECT * from dc_user
    </select>

</mapper>